# FastAPI 核心依赖
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# HTTP 客户端
aiohttp==3.9.1        # 异步HTTP客户端（您的代码中使用的是aiohttp）
requests==2.31.0      # 同步HTTP客户端（备用）

# 环境变量管理
python-dotenv==1.0.0

# 表单处理
python-multipart==0.0.6

# 其他工具
asyncio==3.4.3        # 异步IO（Python内置，但明确列出版本）
uuid==1.30            # UUID生成